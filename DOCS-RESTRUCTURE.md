# 产品文档「单文件 → 产品文件夹」迁移方案

> 目标：产品页从 `分类/<型号>.md` 单文件，迁移为 `分类/<型号>/` 文件夹（`index.md` 概览 + 子主题页面），支持一个产品挂多份文档并在侧边栏折叠展示。
> 状态：待评审，未实施。

## 1. 目标结构

```
迁移前                                  迁移后
sensors_docs/60GHz康养/                sensors_docs/60GHz康养/
├── edv28a.md          →               ├── edv28a/
│   (716 行单页)                        │   ├── index.md      概览/规格/FAQ（URL 不变）
                                        │   ├── mqtt.md       MQTT 对接协议（第 1–5 章）
                                        │   └── self-hosting.md 自建服务器指南（第 6 章）
                                        └── edv21c-w-01/
                                            ├── index.md
                                            └── mqtt.md
```

## 2. 关键设计决策（评审重点）

| # | 决策 | 理由 |
|---|------|------|
| D1 | **文件夹名沿用现有文件名**（`edv28a.md` → `edv28a/index.md`） | Docusaurus 中 `index.md` 映射到所在目录路由，**产品主页 URL 完全不变**（`/sensors/60GHz康养/edv28a`），旧链接/搜索索引/intro.md 里的跳转全部继续有效 |
| D2 | **版本化快照原地同步重构**（current 与 `*-versioned_docs/version-1.0.0` 同一提交内镜像修改） | 线上正式路由由 version-1.0.0 提供；沿用 EDV21C/EDV28A 已确立的"双份同步"约定。若改为"切新版本 1.1.0"，旧版本会挪到 `/1.0.0/` 子路径，**造成全站旧链接失效**，故不采用 |
| D3 | **全部产品统一文件夹化**（sensors 20 + modules 30 + accessories 4 = 54 个），而非只动有子文档的产品 | 避免长期存在两种目录形态；机械移动由脚本完成，成本低 |
| D4 | **子页面拆分只做 2 个长文档**（EDV28A 716 行、EDV21C 400 行） | 其余 52 个产品仍是骨架页，index.md 即全部内容，无需拆分 |
| D5 | **子页面命名用语义文件名**（`mqtt.md`、`self-hosting.md`），标题写入 frontmatter | URL 形如 `/sensors/60GHz康养/edv28a/mqtt`，侧边栏按 frontmatter title 显示 |
| D6 | **CMS 的 `index_file` 从 `_index` 改为 `index`** | Decap 新建产品文件夹时生成 Docusaurus 认识的 `index.md`（当前 `_index` 与 Docusaurus 约定不匹配） |

## 3. 不需要改动的部分

- `docusaurus.config.ts`（docs 插件 path/routeBasePath 不变）
- `sidebars.*.ts`（仍是 `autogenerated`，文件夹→折叠分类是自动行为；含 `index.md` 的目录自动成为"链接到 index 文档"的分类）
- 各分类级 `_category_.json`（60GHz康养 等，位置/折叠配置原样保留）
- `*-versioned_sidebars/*.json`（内容只是 autogenerated 声明，构建时按新目录重新生成）
- i18n（en 侧仅 intro.md，无产品页翻译；产品页回退源语言内容）
- 页面内 FAQ 链接、intro.md 的产品链接（均为绝对 URL，D1 保证不变）

## 4. 实施阶段

### 阶段 1：机械迁移脚本 `scripts/migrate-to-product-folders.js`
- 输入：三条产品线的 current + versioned 目录
- 动作：`X/<分类>/<型号>.md` → `X/<分类>/<型号>/index.md`（git mv 保历史；54×2 份）
- 幂等：已存在目标文件夹则跳过；移动前打印清单，支持 `--dry-run`
- 注：版本化目录逐目录镜像（versioned 里的文件与 current 文件名一致）

### 阶段 2：拆分 2 个长文档（有编辑决策，单独提交）
- **EDV28A** → `index.md`（核心特点/规格参数/FAQ + 「文档导航」列表）+ `mqtt.md`（第 1–5 章）+ `self-hosting.md`（第 6 章）
- **EDV21C** → `index.md` + `mqtt.md`
- frontmatter 补 `title` 与 `sidebar_position`（index=1，子页 2、3）
- index 页加「文档导航」小节（链接到子页面）

### 阶段 3：更新生成/同步脚本的目标路径
- `scripts/regen-sensors.js`：`<分类>/<file>.md` → `<分类>/<file>/index.md`
- `scripts/gen-sensor-docs.js`：平铺写入 → `<分组>/<型号>/index.md`
- `scripts/sync-from-dingtalk.js`：`possiblePaths` 增加文件夹路径（`<dir>/<file>/index.md`），新产品建文件夹
- `scripts/merge-edv28a-doc.js` / 借鉴 merge-mqtt 流程：目标改为子页面文件（mqtt.md）

### 阶段 4：CMS 配置（`static/admin/config.yml`）
- 三个产品线集合 `nested.depth: 2 → 3`（分组/产品/子文档）
- `meta.path.index_file: "_index" → "index"`，使后台新建产品文件夹时生成 Docusaurus 兼容的 index.md
- 字段 schema 不变（产品字段绑定 index.md）

### 阶段 5：验证与文档
- `pnpm run build`（zh-Hans + en 双 locale 通过）
- 抽查产物：`build/sensors/60GHz康养/edv28a/index.html`（主页 URL 不变）与 `.../edv28a/mqtt/index.html`（新子页）均存在且含内容
- 侧边栏：产品为可折叠节点，下挂「概览/MQTT 对接协议/自建服务器指南」
- 更新 `DOCS-GUIDE.md`：新增目录结构约定（一个产品一个文件夹；子文档命名建议：`mqtt` / `protocol` / `self-hosting` / `datasheet`…）
- 提交拆分：①机械迁移 commit ②长文档拆分 commit ③脚本+CMS+指南 commit（便于回滚定位）

## 5. 验证清单

- [ ] 旧 URL `/sensors/60GHz康养/edv28a` 返回 200 且内容为主页（非 404/跳转）
- [ ] 新子页 URL 可访问，右侧 TOC 正常
- [ ] 侧边栏：分类折叠 → 产品折叠 → 子页三级展开正常
- [ ] 本地搜索能搜到子页面内容
- [ ] en 路由 `/en/sensors/...` 构建通过
- [ ] CMS 后台（/admin/）列表仍能显示产品、可新建分组与产品
- [ ] `node scripts/regen-sensors.js` 干跑验证写新路径（不覆盖拆分后的内容——regen 会重写 index.md，需先确认其仅更新骨架字段还是整页覆盖，必要时调整）

## 6. 风险与回滚

| 风险 | 缓解 |
|------|------|
| regen/sync 脚本与拆分后的 index.md 冲突（整页覆盖会抹掉子页面导航） | 阶段 3 重新审查这些脚本的覆盖语义；拆分提交放在脚本适配之前完成并锁定内容 |
| 历史提交里的路径引用失效 | git mv 保留 rename 历史；文件内无相对路径引用（均为绝对 URL），影响面小 |
| 双仓库（GitHub+Gitee）部署差异 | 与现有流程一致，双推即可 |
| 回滚 | 每阶段独立 commit，`git revert` 单个提交即可回滚；URL 未变，无需旧链重定向 |

## 7. 工作量估计

| 阶段 | 内容 | 预估 |
|------|------|------|
| 1 | 迁移脚本 + 执行 + 双 locale 构建验证 | 0.5 天 |
| 2 | EDV28A/EDV21C 内容拆分 | 0.5 天 |
| 3 | 三个生成脚本适配 | 0.5 天 |
| 4–5 | CMS 配置 + 全量验证 + 指南更新 | 0.5 天 |

合计约 2 个工作日（含验证）。
