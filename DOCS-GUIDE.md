# EasyDetek 文档维护手册

本手册面向**文档编辑人员**（产品经理、技术写作、管理员），介绍如何编辑和发布产品文档。

---

## 站点结构概览

```
产品线（导航栏入口）    目录                当前内容
─────────────────────────────────────────────────────
模组                  modules_docs/        30 款（按频段分组）
 ├── 5.8GHz/          5.8GHz 模组（14款）
 ├── 10.5GHz/         10.5GHz 模组（1款）
 ├── 24GHz/           24GHz 模组（13款）
 └── 60GHz/           60GHz 模组（2款）

传感器                sensors_docs/        20 款（按协议分组）
 ├── AC强电/          AC 强电输出（7款）
 ├── Tuya(涂鸦)/      Tuya Zigbee（4款）
 ├── Mijia(米家)/     米家平台（2款）
 ├── BLE蓝牙/         BLE 蓝牙（3款）
 ├── DC干接点/        DC 干接点（2款）
 └── 60GHz康养/       60GHz 康养监护（2款）

配件                  accessories_docs/    4 款
开发者                opensource_docs/     待填充
```

---

## 方案 B：GitHub/Gitee 网页编辑（日常小修改）

**适合：** 修改单个产品的参数、修正错别字、更新描述等。

### 操作步骤

1. **打开仓库**（二选一）：
   - GitHub：https://github.com/easydetek/easydetek.github.io
   - Gitee：https://gitee.com/easydetek/easydetek.gitee.io

2. **找到要编辑的文件**：
   - 进入对应产品线目录（如 `modules_docs/5.8GHz/`）
   - 点击要修改的 `.md` 文件（如 `edc116.md`）

3. **点击编辑**：
   - 点右上角 **✏️ 铅笔图标**（GitHub）或 **「编辑」按钮**（Gitee）

4. **修改内容**（Markdown 格式）：
   ```markdown
   ## 规格参数

   | 参数 | 规格 |
   |------|------|
   | 工作频段 | 5.8GHz |
   | 感应距离 | 6-9m |      ← 直接改这里的数值
   ```

5. **提交**：
   - 填写修改说明（如"更新 EDC116 感应距离参数"）
   - 点 **Commit changes / 提交**

6. **自动生效**：
   - 提交后 GitHub Actions 自动构建部署（约 3-5 分钟）
   - Gitee 需通知开发者手动重建

### Markdown 速查

文档人员只需掌握这几个 Markdown 语法：

| 语法 | 效果 | 示例 |
|------|------|------|
| `# 标题` | 一级标题 | `# EDC116` |
| `## 标题` | 二级标题 | `## 规格参数` |
| `**粗体**` | **粗体** | `**5.8GHz**` |
| `[文字](链接)` | 超链接 | `[规格书](/pdf/edc116.pdf)` |
| `\|列\|列\|` | 表格 | 见下方 |

表格示例：
```markdown
| 参数 | 规格 |
|------|------|
| 工作频段 | 5.8GHz |
| 输入电压 | 7-12V |
```

---

## 方案 C：钉钉知识库批量同步（大批量更新）

**适合：** 新增产品、批量更新参数、产品线大调整。

### 工作流

```
文档人员                    开发者/管理员
   │                            │
   ├─ 在钉钉知识库更新产品清单    │
   │  （产品清单表格）           │
   │                            │
   ├─ 通知开发者「数据已更新」    │
   │                            │
   │                            ├─ 运行同步脚本
   │                            │  node scripts/sync-from-dingtalk.js
   │                            │
   │                            ├─ 检查新产品归类
   │                            │
   │                            ├─ 重建站点
   │                            │  docker compose up -d --build
   │                            │
   └─ 确认站点已更新 ◄───────────┘
```

### 同步脚本用法

```bash
# 1. 确保已登录钉钉 CLI
dws auth login

# 2. 运行同步脚本
node scripts/sync-from-dingtalk.js

# 3. 检查新产品（脚本会列出，需手动归类到频段/协议子目录）

# 4. 重建站点
docker compose -f docker-compose.local.yml up -d --build
```

脚本会自动：
- 从钉钉读取模组、传感器、配件三个产品清单
- 解析表格数据（型号、参数、应用信息）
- 生成/更新对应的 Markdown 文档

---

## 新增一个产品型号

### 方式 1：钉钉表格新增（推荐大批量）

1. 在钉钉知识库的对应产品清单表格里添加一行
2. 填写型号、参数、应用信息
3. 运行同步脚本

### 方式 2：手动新建文件（单个产品）

1. 在对应产品线目录的子目录下新建 `.md` 文件：
   ```
   modules_docs/5.8GHz/edv999.md
   ```

2. 填写内容（参考模板）：
   ```markdown
   ---
   sidebar_position: 5
   ---

   # EDV999

   > 产品特点｜5.8GHz 模组

   ## 核心特点

   超高性能微波感应模组

   ## 规格参数

   | 参数 | 规格 |
   |------|------|
   | 工作频段 | 5.8GHz |
   | 感应距离 | 8-12m |
   | 输入电压 | 7-12V |
   | 输出方式 | IO/UART/PWM |
   | 外形尺寸 | 20×22mm |

   ## 应用信息

   | 项目 | 说明 |
   |------|------|
   | 细分市场 | 光源和家居 |
   | 安装方式 | 常规3m |
   | 应用场景 | 吸顶灯、筒灯 |

   :::info 规格书
   完整规格书请联系 support@easydetek.com。
   :::
   ```

3. 提交并重建

---

## 常见问题

### Q: 文件名怎么命名？
型号小写，非字母数字用连字符。如 `EDC116` → `edc116.md`，`EDV163-N-01` → `edv163-n-01.md`。

### Q: 产品放到哪个子目录？
- 模组按频段：`5.8GHz/` `10.5GHz/` `24GHz/` `60GHz/`
- 传感器按协议：`AC强电/` `Tuya(涂鸦)/` `Mijia(米家)/` `BLE蓝牙/` `DC干接点/` `60GHz康养/`

### Q: 修改后多久生效？
- GitHub 网页编辑：提交后 3-5 分钟自动生效
- 钉钉同步：运行脚本 + 重建后立即生效

### Q: 改错了怎么办？
Git 有完整版本历史，任何修改都可以回退。联系开发者恢复历史版本。

### Q: 需要帮助？
- 技术支持：support@easydetek.com
- 文档有问题：点击任意文档底部的「👎 需改进」反馈
